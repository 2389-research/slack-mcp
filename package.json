{
  "name": "@2389/slack-mcp",
  "version": "1.0.0",
  "description": "MCP server for Slack workspace integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "pretest": "npm run build",
    "test": "node --test tests/protocol.test.mjs tests/unit.test.mjs",
    "test:e2e": "node --test tests/e2e.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@slack/web-api": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
